# Common scripts

General utilities, most of the functions are needed for the 2 approaches, i.e decoding videos and representation learning.


Moreover, please note that:

-`logistic_regression.py` is a is a self-contained script that can be used to run the simple logistic regression approach.

-`run_predict_subjects.py` it can be used to perform the task of predicting the subject code.

